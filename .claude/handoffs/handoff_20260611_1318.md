# HANDOFF — 2026-06-11 — 13:16

## ✅ Completado en esta sesión
- Migración de lo útil de `cowork-agentkit-nuevo` → `psicoeduca-agente`, y eliminación del proyecto viejo.
- `config/prompts.yaml` y `config/business.yaml` creados (personalidad de Skinner + datos del negocio + menú de bienvenida).
- Repo GitHub `Jeancle10/psicoeduca-agente` creado y código pusheado.
- Despliegue en Railway: servicio online en `https://psicoeduca-agente-production.up.railway.app`.
- Detectado y resuelto: `railway up` no subía `config/*.yaml` (gitignored) → se creó `docker-entrypoint.sh` que los reconstruye desde variables `PROMPTS_YAML_B64` / `BUSINESS_YAML_B64`. Verificado con pruebas al webhook: Skinner responde con datos reales de PsicoEduca.
- Conexión Meta WhatsApp completa (pasos 2-6 de `docs/guia-conexion-meta.md`):
  - App "Psicoeduca" reutilizada, número real +595982469351 agregado (Phone Number ID `1096140520250334`)
  - Webhook verificado, suscripción a `messages` activa
  - Usuario del sistema "skinner-agente" creado, token permanente `META_ACCESS_TOKEN` generado
  - `META_ACCESS_TOKEN` y `META_PHONE_NUMBER_ID` cargados en Railway, contenedor reiniciado

## 🔄 En progreso (quedó a medias)
- **Paso 7 (prueba final)**: Jean tiene que escribirle un WhatsApp real a +595982469351 ("Hola") y confirmar que Skinner responde. Se cortó la sesión justo antes de hacer esta prueba.

## ⏳ Próxima sesión — primer paso EXACTO
1. Pedirle a Jean que mande un WhatsApp a **+595982469351** (mensaje "Hola")
2. Correr `cd "C:\Users\MI PC\proyectos\psicoeduca-agente" && railway logs` y revisar:
   - Que aparezca `Mensaje de <numero>: Hola`
   - Que NO aparezca `Faltan META_ACCESS_TOKEN o META_PHONE_NUMBER_ID`
   - Que el mensaje de Skinner se haya enviado (sin error de la API de Meta)
3. Si responde bien → 🎉 agente en producción, actualizar `memory/progreso.md` a "Skinner en producción, conectado a WhatsApp real"
4. Si falla, revisar `docs/guia-conexion-meta.md` sección "Problemas típicos" (token, suscripción `messages`, mismatch de Phone Number ID)
5. Pendiente menor: decidir qué hacer con el turno de prueba `JUE-2026-06-18-1830` reservado a nombre de "Jean Clemotte" en Airtable (dato de prueba de la simulación anterior)

## ⚠️ Errores encontrados hoy
- `railway up` excluye archivos listados en `.gitignore` (no hay forma de hacer que `.railwayignore` los "rescate" — railway ignora ambos para ese tipo de exclusión de negocio). Se resolvió inyectando esos archivos vía variables de entorno + `docker-entrypoint.sh` en cada arranque del contenedor. Esto queda documentado para futuros deploys: si se edita `config/prompts.yaml` o `config/business.yaml`, hay que regenerar `PROMPTS_YAML_B64` / `BUSINESS_YAML_B64` y volver a cargarlos en Railway.

## 🧠 Decisiones tomadas
- No crear una 4ta app de Meta duplicada — se reutilizó la app "Psicoeduca" existente (ID `1658526415289976`), ya asociada al Business Manager correcto.
- Verificación del negocio (Paso 3 de Meta) se salteó por ahora — no es obligatoria para operar, solo para subir límites de mensajería a futuro.
- Token permanente con expiración "Nunca" (vía Usuario del Sistema "skinner-agente"), no el token temporal de 24hs del API Setup.

## 📁 Archivos modificados
- `proyectos/psicoeduca-agente/Dockerfile`
- `proyectos/psicoeduca-agente/docker-entrypoint.sh` (nuevo)
- `proyectos/psicoeduca-agente/.railwayignore` (nuevo)
- `proyectos/psicoeduca-agente/config/prompts.yaml`, `config/business.yaml` (nuevos, gitignored)
- Variables Railway: `META_ACCESS_TOKEN`, `META_PHONE_NUMBER_ID`, `PROMPTS_YAML_B64`, `BUSINESS_YAML_B64`
- `bitacora/2026-06-11.md` (nuevo)
