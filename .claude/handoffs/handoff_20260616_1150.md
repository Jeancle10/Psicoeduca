# HANDOFF — 2026-06-16 — 11:44

## ✅ Completado en esta sesión

- Proyecto Ñakurutu creado completo en `C:\Users\MI PC\proyectos\Ñakurutu\`
  - `main.py` (menú CLI), `src/` (5 módulos), `config.json`, `requirements.txt`, `.gitignore`, `README.md`
- 5 tablas creadas en Airtable via MCP (base `appcetoe3cXohXKUb` — "Ñakurutu Historico"):
  - Alumnos `tbltarCpEDUGJqSo2`, Períodos `tblroKKt6Irmwo0Sq`, Inscripciones `tblyaV1YDh0v0QlBS`, Pagos `tblqQKXVmCTqm366s`, Cohortes `tblaNSwteKhzGzXUO`
  - Cohortes incluye 3 fórmulas activas: Tasa Retención %, Ingresos Totales Esperados, % Recaudación
- Carga del archivo `Ñk 24-25.xlsx` → Airtable exitosa:
  - 178 alumnos nuevos, 206 inscripciones, 749 pagos, **0 errores**
  - 28 alumnos detectados en múltiples períodos (Intensivo + Extensivo) — no duplicados

## 🔄 En progreso (quedó a medias)

- **Carga de años anteriores** — archivos históricos (2014–2023) en la carpeta pero no cargados:
  - `C:\Users\MI PC\proyectos\Ñakurutu\Copia Planillas presupuestos\`
  - `2014.xlsx`, `2016.xlsx`, `2017.xlsx`, `2018.xlsx`, `2019 Ext.xlsx`, `2019 Int.xlsx`, `2020.xlsx`, `2021.xlsx`, `2022.xlsx`, `Extensivo Virtual 2021.xlsx`, `Ñk 22-23.xlsx`

- **Skinner (psicoeduca-agente)** — sigue pendiente la prueba final de WhatsApp real al +595982469351

## ⏳ Próxima sesión — primer paso EXACTO

**Si es Ñakurutu:**
1. `cd "C:\Users\MI PC\proyectos\Ñakurutu"`
2. Leer headers del siguiente archivo: `python -c "import pandas as pd; df = pd.read_excel('Copia Planillas presupuestos/Ñk 22-23.xlsx', nrows=3, dtype=str); print(df.head(3).to_string())"`
3. Adaptar `carga_24_25.py` para ese archivo (puede tener columnas distintas)
4. Cargar, verificar en Airtable, continuar con siguiente año

**Si es Skinner:**
1. Jean manda WhatsApp "Hola" a +595982469351
2. Correr `railway logs` en `C:\Users\MI PC\proyectos\psicoeduca-agente`
3. Verificar que Skinner responde

## ⚠️ Errores encontrados hoy

1. **UnicodeEncodeError (cp1252)** — caracteres `═` (U+2550) no compatibles con terminal Windows
   - Solución: `sys.stdout.reconfigure(encoding='utf-8')` + reemplazar con ASCII (`=`, `-`)
2. **403 Forbidden Airtable API** — PAT válido pero workspace no asignado
   - Síntoma: `GET /meta/bases` devuelve `{"bases":[]}`
   - Solución: airtable.com → Account → Developer hub → editar PAT → agregar workspace "My First Workspace" + scopes `data.records:read` y `data.records:write`

## 🧠 Decisiones tomadas

- Script dedicado por archivo Excel (`carga_24_25.py`) — más confiable que script genérico porque cada año puede tener estructura diferente
- Celular: prepend `+595 0` a los dígitos (93307386 → +595 093307386)
- Monto Debido fijo = 450000 en todos los pagos
- Tuvo Descuento? = True cuando monto > 0 y < 450000
- Rollup fields en Cohortes no se pueden crear via API → campos numéricos que el script llena manualmente
- Rate limit Airtable: 0.22s entre requests + retry con backoff exponencial para 429

## 📁 Archivos modificados/creados

```
C:\Users\MI PC\proyectos\Ñakurutu\
├── main.py                    (NUEVO)
├── carga_24_25.py             (NUEVO)
├── config.json                (NUEVO — token Airtable, NO commitear)
├── requirements.txt           (NUEVO)
├── .gitignore                 (NUEVO)
├── README.md                  (NUEVO)
└── src/  (airtable_client.py, excel_parser.py, data_cleaner.py,
           report_generator.py, image_generator.py, __init__.py)  (NUEVO)

c:\Users\MI PC\psicoeduca\
├── bitacora/2026-06-16.md     (NUEVO)
└── memory/progreso.md         (ACTUALIZADO)
```
