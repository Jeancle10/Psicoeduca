# Bitácora — 2026-06-11

## Resumen del día — Agente Skinner: integración final + conexión Meta WhatsApp

### 1. Limpieza de proyectos
- Se revisó `cowork-agentkit-nuevo` y se traspasó todo lo útil (prompts, configuración de negocio) a `psicoeduca-agente`.
- Se eliminó `cowork-agentkit-nuevo` una vez extraído lo necesario. Queda un solo proyecto: `psicoeduca-agente`.

### 2. Configuración de Skinner
- Se creó `config/prompts.yaml` (personalidad de Skinner: tono, servicios, precios, horarios, política de cancelación, instrucciones de uso de herramientas Airtable, protocolo de crisis, enfoque clínico ACT/DBT).
- Se creó `config/business.yaml` (datos estructurados del negocio + menú de bienvenida con botones de WhatsApp).
- Ambos archivos están en `.gitignore` (datos sensibles del negocio, no van a git).

### 3. Repo en GitHub
- Se creó `https://github.com/Jeancle10/psicoeduca-agente` (privado).
- Se corrigió el `origin` (apuntaba mal al repo del template `ivanlafuentepy/cowork-agentkit`).
- Push del código (brain.py con tool_use, herramientas Airtable, generador de turnos).

### 4. Despliegue en Railway
- Proyecto `psicoeduca-agente` creado en Railway, servicio desplegado vía Dockerfile.
- Variables cargadas: `ANTHROPIC_API_KEY`, `AIRTABLE_API_KEY`, `AIRTABLE_BASE_ID`, `WHATSAPP_PROVIDER=meta`, `META_VERIFY_TOKEN`, `ENVIRONMENT`, `DATABASE_URL`.
- Dominio público generado: `https://psicoeduca-agente-production.up.railway.app`
- **Problema detectado**: `railway up` excluye archivos del `.gitignore`, así que `config/prompts.yaml` y `config/business.yaml` NO llegaban al contenedor → Skinner respondía con un prompt genérico, sin menú ni datos del negocio.
- **Solución**: se creó `docker-entrypoint.sh` que reconstruye esos dos archivos desde variables de entorno (`PROMPTS_YAML_B64`, `BUSINESS_YAML_B64`, contenido en base64) al arrancar el contenedor. Se actualizó el `Dockerfile` para usarlo.
- Se verificó con mensajes de prueba al webhook: Skinner ahora responde con precios, servicios y menú correctos. ✅

### 5. Conexión a Meta WhatsApp Cloud API (Pasos 2-6 de la guía)
- Se reutilizó la app existente "Psicoeduca" (ID `1658526415289976`) en developers.facebook.com, ya asociada al Business Manager "PsicoEduca".
- Se agregó el número real del consultorio: **+595982469351** → Phone Number ID: `1096140520250334`
- Webhook configurado y verificado: URL `https://psicoeduca-agente-production.up.railway.app/webhook`, verify token `psicoeduca-skinner-verify-2026` ✅
- Suscripción al campo `messages` activada ✅
- Se creó usuario del sistema "skinner-agente" en Business Settings, con acceso de control total a la cuenta de WhatsApp Business y a la app.
- Se generó **token permanente** (`META_ACCESS_TOKEN`, sin expiración) con permiso `whatsapp_business_messaging`.
- Se cargaron `META_ACCESS_TOKEN` y `META_PHONE_NUMBER_ID` en Railway → contenedor reiniciado con las nuevas variables.

## 🔄 En progreso
- Falta la **prueba final end-to-end**: Jean tiene que escribirle por WhatsApp a +595982469351 y confirmar que Skinner responde. Quedó pendiente justo antes del cierre.

## 📁 Archivos modificados
- `proyectos/psicoeduca-agente/Dockerfile` (entrypoint)
- `proyectos/psicoeduca-agente/docker-entrypoint.sh` (nuevo)
- `proyectos/psicoeduca-agente/.railwayignore` (nuevo)
- `proyectos/psicoeduca-agente/config/prompts.yaml`, `config/business.yaml` (nuevos, gitignored)
- Variables de Railway: `META_ACCESS_TOKEN`, `META_PHONE_NUMBER_ID`, `PROMPTS_YAML_B64`, `BUSINESS_YAML_B64`
